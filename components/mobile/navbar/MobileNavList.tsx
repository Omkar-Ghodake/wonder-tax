import { NavLinkType } from '@/types/dataTypes'
import React from 'react'

const MobileNavList = ({ list }: { list: NavLinkType }) => {
  return <ul>MobileNavList</ul>
}

export default MobileNavList
